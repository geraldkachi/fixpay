spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${TEST_JWK_SET_URI:http://localhost:9999/mock-jwks}
  cloud:
    gateway:
      routes:
        - id: core-test
          uri: ${CORE_STUB_URL:http://localhost:18081}
          predicates:
            - Path=/smoke/**
